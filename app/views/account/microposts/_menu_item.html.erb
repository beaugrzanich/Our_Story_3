<% if can? :edit, current_user %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_user]),
    label: t('microposts.newpost'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="fal fa-note ti ti-notepad"></i>
    <% end %>
  <% end %>
<% end %>
